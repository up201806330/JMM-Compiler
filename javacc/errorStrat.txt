void WhileStatement(): {} {
    <While> <OpenParenthesis>
    try{
        (Expression() #WhileCondition <CloseParenthesis>)
        Statement() #WhileBody
    }
    catch(ParseException e){
        reports.add(new Report(ReportType.ERROR, Stage.SYNTATIC, token.beginLine, e.getMessage()));
        Token current = error_skipto(OpenBlock, Semicolon);
        if(current == OpenBlock){
            WhileBody();
        }else if(current == Semicolon){
            if(current.next == CloseParenthesis){
                current = current.next;
                while (current == CloseParenthesis){
                    current = current.next;
                }
                WhileBody();
            }
            
        }
    }

    
}

void WhileBody():{}
{
    Statement;
}